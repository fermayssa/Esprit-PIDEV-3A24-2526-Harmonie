<form method="post" action="{{ path('app_session_meditation_delete', {'id': session_meditation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ session_meditation.id) }}">
    <button class="btn">Delete</button>
</form>
