{% extends 'base.html.twig' %}

{% block title %}SessionMeditation{% endblock %}

{% block content %}
    <h1>SessionMeditation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ session_meditation.id }}</td>
            </tr>
            <tr>
                <th>Auteur</th>
                <td>{{ session_meditation.auteur }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ session_meditation.duree }}</td>
            </tr>
            <tr>
                <th>Theme</th>
                <td>{{ session_meditation.theme }}</td>
            </tr>
            <tr>
                <th>AudioUrl</th>
                <td>{{ session_meditation.audioUrl }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_session_meditation_index') }}">back to list</a>

    <a href="{{ path('app_session_meditation_edit', {'id': session_meditation.id}) }}">edit</a>

    {{ include('session_meditation/_delete_form.html.twig') }}
{% endblock %}
