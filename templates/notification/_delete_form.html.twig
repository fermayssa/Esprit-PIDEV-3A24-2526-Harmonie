<form method="post" action="{{ path('app_notification_delete', {'idNotification': notification.idNotification}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ notification.idNotification) }}">
    <button class="btn">Delete</button>
</form>
