<form method="post" action="{{ path('app_saved_course_delete', {'id': saved_course.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ saved_course.id) }}">
    <button class="btn">Delete</button>
</form>
