<form method="post" action="{{ path('app_password_reset_code_delete', {'id': password_reset_code.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ password_reset_code.id) }}">
    <button class="btn">Delete</button>
</form>
