<form method="post" action="{{ path('app_sommeil_delete', {'idSommeil': sommeil.idSommeil}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sommeil.idSommeil) }}">
    <button class="btn">Delete</button>
</form>
