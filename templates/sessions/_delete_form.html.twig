<form method="post" action="{{ path('app_sessions_delete', {'sessionId': session.sessionId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ session.sessionId) }}">
    <button class="btn">Delete</button>
</form>
