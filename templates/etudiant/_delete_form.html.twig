<form method="post" action="{{ path('app_etudiant_delete', {'idEtudiant': etudiant.idEtudiant}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etudiant.idEtudiant) }}">
    <button class="btn">Delete</button>
</form>
