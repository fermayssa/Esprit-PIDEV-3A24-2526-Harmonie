<form method="post" action="{{ path('app_reaction_delete', {'idReaction': reaction.idReaction}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reaction.idReaction) }}">
    <button class="btn">Delete</button>
</form>
