<form method="post" action="{{ path('app_consommation_delete', {'idConsommation': consommation.idConsommation}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ consommation.idConsommation) }}">
    <button class="btn">Delete</button>
</form>
