{% extends 'base.html.twig' %}

{% block title %}Exercice{% endblock %}

{% block content %}
    <h1>Exercice</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdExercice</th>
                <td>{{ exercice.idExercice }}</td>
            </tr>
            <tr>
                <th>NomExercice</th>
                <td>{{ exercice.nomExercice }}</td>
            </tr>
            <tr>
                <th>TypeExercice</th>
                <td>{{ exercice.typeExercice }}</td>
            </tr>
            <tr>
                <th>VideoExercice</th>
                <td>{{ exercice.videoExercice }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_exercice_index') }}">back to list</a>

    <a href="{{ path('app_exercice_edit', {'idExercice': exercice.idExercice}) }}">edit</a>

    {{ include('exercice/_delete_form.html.twig') }}
{% endblock %}
